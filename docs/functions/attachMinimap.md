[atomic-core](../README.md) / attachMinimap

# Function: attachMinimap()

> **attachMinimap**(`game`, `canvas`, `opts?`): `void`

Defined in: [api/createGame.ts:1291](https://github.com/philbgarner/atomic-core/blob/00ebe2c72dacab39c637c5b68506af1715bce7d0/src/lib/api/createGame.ts#L1291)

Wire up a 2D canvas minimap that redraws on every `turn` event.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `game` | `GameHandle` |
| `canvas` | `HTMLCanvasElement` |
| `opts` | `MinimapOptions` |

## Returns

`void`
