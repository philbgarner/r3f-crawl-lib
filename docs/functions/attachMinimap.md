[atomic-core](../README.md) / attachMinimap

# Function: attachMinimap()

> **attachMinimap**(`game`, `canvas`, `opts?`): `void`

Defined in: [api/createGame.ts:1456](https://github.com/philbgarner/atomic-core/blob/059d282bcb55e802a623f9e7a0f2cb290623baf0/src/lib/api/createGame.ts#L1456)

Wire up a 2D canvas minimap that redraws on every `turn` event.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `game` | `GameHandle` |
| `canvas` | `HTMLCanvasElement` |
| `opts` | `MinimapOptions` |

## Returns

`void`
