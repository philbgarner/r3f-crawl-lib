[atomic-core](../README.md) / attachMinimap

# Function: attachMinimap()

> **attachMinimap**(`game`, `canvas`, `opts?`): `void`

Defined in: [api/createGame.ts:1502](https://github.com/philbgarner/atomic-core/blob/4041e6411d0bb6dd169f8ed8eae77a3af59aedf0/src/lib/api/createGame.ts#L1502)

Wire up a 2D canvas minimap that redraws on every `turn` event.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `game` | `GameHandle` |
| `canvas` | `HTMLCanvasElement` |
| `opts` | `MinimapOptions` |

## Returns

`void`
